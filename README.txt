This program consists of 3 algoritms: LFSR, Geffe Generator, RC4.
LFSR is for register with length 25. But this value can be easily changes with help of const.
The same was correct for Geffe Generator(with help of const values you can change the length of registers).